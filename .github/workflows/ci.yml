name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build_test:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install deps
        run: |
          echo "📦 Installing dependencies..."
          npm ci
          echo "✅ Dependencies installed successfully"
          echo "📋 Installed packages:"
          npm list --depth=0

      # 1. 先做最便宜、最容易失败的检查（快失败）
      - name: Format check (changed files)
        run: npm run format:check:changed

      - name: Lint
        run: npm run lint

      # 2. 再跑模板 / 逻辑测试（你现在失败的就是这里）
      - name: Unit tests
        run: npm test

      # 3. 最后构建
      - name: Build
        env:
          # CI 环境增加超时时间，避免网络慢导致超时
          PROJECTS_FETCH_TIMEOUT: 30000 # 30秒单请求超时（默认10秒）
          RSS_FETCH_TIMEOUT: 20000 # 20秒单请求超时（默认10秒）
          RSS_TOTAL_TIMEOUT: 180000 # 3分钟总超时（默认60秒）
        run: npm run build

      - name: Show build artifacts on failure
        if: failure()
        run: |
          echo "=== Build failed - checking dist directory ==="
          ls -la dist/ || echo "dist directory not found"
          echo "=== Checking dev cache directory ==="
          ls -la dev/ 2>/dev/null || echo "dev directory not found"
